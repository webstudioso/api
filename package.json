{
  "name": "webstudio-api",
  "version": "0.1.0",
  "bin": {
    "webstudio-api": "bin/webstudio-api.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "husky": {
    "hooks": {
      "pre-commit": "cdk bootstrap"
    }
  },
  "devDependencies": {
    "aws-cdk": "^2.49.0",
    "husky": "^8.0.1",
    "jest": "^27.5.1"
  },
  "dependencies": {
    "aws-cdk-lib": "2.49.0",
    "constructs": "^10.0.0"
  }
}
